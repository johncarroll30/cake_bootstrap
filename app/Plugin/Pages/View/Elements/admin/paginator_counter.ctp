<?php /* if ($paginator->counter(array('format' => '%pages%')) > 1): ?>
<p><?php
	echo $paginator->counter(array(
	'format' => __d('pages', 'Page %page%. Total pages - %pages%. Showing - %current% of %count%.', true)
	));
?></p>
<?php endif; */?>
